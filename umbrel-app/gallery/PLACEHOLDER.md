# Gallery placeholder

Three screenshots are required before Umbrel App Store submission:

- `1.jpg` — the earnings dashboard (the headline view; "X sats earned this month")
- `2.jpg` — the 5-step setup wizard mid-flow (e.g. step 3, LNbits connect)
- `3.jpg` — the federation tab or the seed-request review queue

Recommended specs (per Umbrel guidelines for similar apps):
- 16:9 aspect ratio
- Minimum 1280×720
- JPEG quality 85+
- Take screenshots from a real Umbrel device for authenticity

Replace this file (and add the three JPEGs) before opening the App Store
submission PR. The validation workflow warns when these are missing.
